input.onButtonPressed(Button.AB, function () {
    for (let index = 0; index < 4; index++) {
        basic.showString("Hello!")
        basic.showString("lilly ")
        basic.showIcon(IconNames.Ghost)
        basic.showIcon(IconNames.Heart)
    }
})
